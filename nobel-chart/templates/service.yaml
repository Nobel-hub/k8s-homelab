apiVersion: v1
kind: Service
metadata:
  name: {{ include "nobel-chart.fullname" . }}
  labels:
    {{- include "nobel-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nobel-chart.selectorLabels" . | nindent 4 }}
